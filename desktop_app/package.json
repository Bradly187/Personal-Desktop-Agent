{
  "name": "desktop-agent-shell",
  "version": "0.1.0",
  "description": "Electron desktop shell for the Personal Desktop Agent: chat + dashboard iframes, file tree, Monaco editor, pty terminal, backend lifecycle owner.",
  "private": true,
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "postinstall": "node scripts/rebuild-pty.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "monaco-editor": "0.56.0",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "electron": "^43.1.0"
  }
}
